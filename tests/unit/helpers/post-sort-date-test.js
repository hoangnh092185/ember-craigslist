
import { postSortDate } from 'craigslist/helpers/post-sort-date';
import { module, test } from 'qunit';

module('Unit | Helper | post sort date');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = postSortDate([42]);
  assert.ok(result);
});

